const os = require('os');

/**
 * 
 * @returns {string | undefined}
 */
function getFirstExternalAddress() {
    return Object.values(os.networkInterfaces()).find(value => value[1].internal === false)?.splice(1)[0].address;
};

module.exports = { getFirstExternalAddress };