const http = require('http');

const hs = require('web-socket-lib/http-server');


class Socket {
    id;
    response;

    constructor(id) {

    }

    emit() {

    }

    on() {

    }
};

/**
 * 
 * @param {} httpServer 
 * @returns 
 */
function createServer(httpServer) {
    const server = httpServer ?? hs.createServer();
    const sockets = [];
    const listeners = [];
    const event = [];

    const interface = {
        addSocket() {

        },

        emit() {

        },

        on() {

        }
    };

    return interface;
}